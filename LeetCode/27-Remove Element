/**
 * @param {number[]} nums
 * @param {number} val
 * @return {number}
 */
var removeElement = function(nums, val) {
    var j=0;
    while (j<nums.length) {
        if(nums[j]===val) {
            nums.splice(j,1);
        }else {
            j++;
        }
        
    }
};
